# Codebase Unification — single app with full functionality

Goal: turn this repo into **one application** that keeps ALL functionality of
both the legacy single-company app (`app/`) and the multi-tenant SaaS
platform (`saas/`), with one frontend (the React SPA in `web/`). The legacy
`app/` tree and the Jinja templates are deleted in the final batch.

## Target architecture (end state)

- **One backend:** `saas/` — the merged app (FastAPI, JWT, per-tenant DBs).
- **One frontend:** `web/` React SPA — all legacy portal features reimplemented as pages.
- **No `saas → app` or `app → saas` imports** — shared code lives in `saas/`.
- Legacy single-company functionality becomes a **statement/credits domain**
  inside every tenant DB (documents, extraction, credits billing, L1/L2
  dedup, AI model config + costing).

## Batch status

| Batch | Scope | Status |
|---|---|---|
| **0** | **Structural unification (cycle break).** `saas/passwords.py` (hashing, incl. legacy SHA-256), `saas/ai_cleanup.py` (Gemini orphan cleanup). All 7 `saas→app` imports removed; `app/security.py` reduced to a deprecated re-export shim. Both apps still import & run. | ✅ DONE |
| **1a** | **Merged extraction stack (done).** Platform schema: `ai_model_routing`, `ai_model_pricing`, `ai_usage_log` (+ indexes) in `saas/platform_db.py`. New shim-free modules: `saas/ai/` (`model_registry`, `pricing`, `gemini_extraction`), `saas/extraction.py`, `saas/ingestion.py`, `saas/worker_pool.py`. Config additions in `saas/config.py` (`GEMINI_MODEL_TEXT_PDF`, `GEMINI_MODEL_VISUAL`, `USD_TO_INR_RATE`, `GEMINI_MAX_PAGES_PER_CHUNK`, `GEMINI_CHUNK_PARALLELISM`, `OCR_WORKER_POOL_SIZE`). Verified: imports, pricing/cost math, mapping, fingerprint, category routing, ingestion URL rewrite, zero `saas→app` imports, DDL rollback-checked on live PG. → ✅ | ✅ |
| **1b** | **Document domain on tenant schema.** Ported the legacy document/credits tables (`uploads`, `extractions`, `parties`, `items`, `manual_verifications`, `credits` singleton wallet, `credit_transactions`, `credit_requests` + indexes + dedup/rejection columns) into `saas/tenant_schema.py` (TENANT_DDL + TENANT_PATCHES, `SCHEMA_VERSION` 3.13.0 → 3.14.0). New modules `saas/credits.py` (wallet, consume/top-up/requests/ledger) and `saas/statements.py` (save_extraction, L1 file-hash + L2 content-fingerprint dedup, period-overlap, quarter helpers), conn-based SaaS style, ported from `app/helpers.py`. Verified 35/35 on a scratch tenant (DDL + per-statement patches + seed + credits/statements flows); zero `saas→app` imports. Deferred (Batch 2 decision): `registrations`/`registration_links`/`password_reset_requests` fold into platform user provisioning + SaaS auth; `handovers` is a field-ops route port; `activity_logs` is already covered by the tenant `audit_logs` table. → ✅ | ✅ |
| **2** | **Legacy routes → saas endpoints.** New `saas/routers/statements.py` — tenant-scoped statement portal (upload + upload-from-url on the OCR worker pool with progress persisted to `uploads.progress_pct/progress_stage`, document list with scope + date/status filters, period-check with L1 file-hash/L2 fingerprint dedup + proceed-with-warning, credits wallet/consume/request, verification agent queue/claim/inline-edit/reject/re-extract/Excel download + corrections/history export). Verification agents are **tenant users** (`manual_verifications.assigned_to/verified_by → users(id)`, least-loaded auto-assign). Superadmin rewrote: `/costing` + new `/costing/export` source platform `ai_usage_log` (platform-wide, per division/model/user with best-effort tenant user-name resolution, monthly trend, recent 200 / export ≤5000 rows); new `/ai-models` (category routing + pricing config) and `/divisions/{did}/credits` wallet admin (allocate/approve/reject with `reviewed_by=None`; actor recorded in `platform_audit_logs`). `/finance` AI spend now reads `ai_usage_log` (per-tenant `ocr_usage` vestigial under merged path). Schema: `saas/tenant_schema.py` Phase 12 — `statement.*` permission module wired into DEFAULT_ROLES (ho/asm verify+upload; nsm…rsm + mr/psr upload+view+credits; verifier/verification_agent verify; auditor/finance view+credits; distributor view) + uploads progress columns, `SCHEMA_VERSION` 3.14.0 → 3.15.0. Port fixes surfaced by smoke tests: `documents()`/`extraction_payload()` legacy `users.role` column replaced by `roles.name` join; `rsm` granted statement codes in seed (fresh provision; existing tenants get the Phase 12 patch at migration). Verified green: imports, 13 routes registered, zero `saas→app` imports, scratch tenant + scratch platform smoke (permissions/role wiring, uploads progress, L1/L2/period dedup, save_extraction, credits wallet incl. superadmin approve `reviewed_by=None`, auto-assign/queue/claim/inline/reject/reset, documents scoping/filters, Excel builders, costing rewrite + export, model routing/pricing CRUD), live PG touched read-only only (no DDL commits; live schema still 3.9.0 pre-deploy). → ✅ | ✅ |
| **3** | **Legacy UI → React SPA.** New tenant pages `/app/statements` (`Statements.jsx` — document list + date/status filters, upload file/URL with L1 dup-confirm + progress polling, extraction result view with parties/items, delete/restore, credits + quarter banners), `/app/statements/credits` (`StatementsCredits.jsx` — wallet, consumption bar, transaction ledger, request-credits), `/app/statements/verify` (`StatementsVerify.jsx` — agent queue/unassigned/history tabs with stats, claim + bulk-claim, task workspace with inline party/item editor, save-&-verify, upload corrected workbook, reject, AI re-extract, Excel download + history export). New superadmin pages `/superadmin/ai-models` (`AiModels.jsx` — per-category model routing overrides + editable USD pricing table + add/delete model) and `/superadmin/divisions/:did/credits` (`DivisionCredits.jsx` — wallet, allocate, pending approve/reject, ledger, per-op usage; mounted as the "Statement Credits" tab on DivisionDetail). `Costing.jsx` gained the Excel export button (4-sheet workbook, honors the days filter). Shared read-only extraction viewers in `web/src/StatementExtraction.jsx` keep both views field-for-field identical. Nav wiring in `ui.jsx` (Statements / Statement Verify / Statement Credits gated on `statement.view/verify/credits`, AI Models under Platform for owner/full) + routes in `App.jsx`. Verified: `vite build` green, every new fetch/mutation matched against the Batch 2 endpoint contracts read back-to-back. Template retirement intentionally deferred to Batch 4 (legacy `app/` keeps serving until then). → ✅ | ✅ |
| **4** | **Delete legacy tree + final sweep.** Removed `app/`, `templates/`, `static/`, `uploads/` (incl. the deprecated `app/security.py` re-export shim and the `app/compat.py` Flask-compat layer). `scripts/test_ai_models.py` ported to the SaaS `/api/v1/superadmin/ai-models*` endpoints — zero `app`-package imports remain anywhere (`saas/` and `scripts/` fully self-contained). Pruned `requirements.txt` (dropped `jinja2`, `itsdangerous`), Dockerfile no longer copies `app/`, legacy-only env vars removed from `.env`, README rewritten for the unified app. **Script suite final shape:** all remaining gates are hermetic (scratch control-plane DB set before importing `saas`, scratch tenant provisioned, both dropped in `finally`) — `smoke_test`, `verify_batch2`, `test_ai_models`, `test_platform_roles`, `test_product_crud` (maintained) plus the five legacy scripts ported to the `/divisions` architecture: `test_dashboards_period` (division provision + `division_slug` login), `test_pages_load` (current SA pages — metrics/divisions/audit-logs/ai-models/costing — and a per-role tenant page sweep), `test_campaign_multibrand` (`/superadmin/companies/{cid}/*` → `/superadmin/divisions/{did}/*`; campaign carries `division_id` so tenant-scope tracking sees it), `test_scoping` (division-bound users; division admin is now division-scoped "team"), `test_roi_analytics` (seeded MR user carries `division_id`; products columns split into `campaign_products`). Retired with coverage absorbed elsewhere: `test_superadmin_flows` (lifecycle covered by smoke), `test_ocr_usage` (route gone — covered by `verify_batch2`/`test_ai_models` costing), the DEMO1234 phase-era scripts (`verify_phase2/3/4/5/67`, `verify_demo`, `verify_gaps`, `verify_bulk`, `test_recovery`, `test_re_extract`, `test_grace_proof`, `test_invoice_proof`, `test_pob_campaign_products`), legacy maintenance scripts (`add_created_by`, `backfill_db_creds`, `backfill_user_index`, `check_cols`, `check_tables`, `diagnose_auto`, `fix_missing_columns`, `reset_stuck_pobs`, `seed_demo`) and both `_probe_*` scratch probes. Route-shadowing bugs found by the pages-load port fixed: `/campaigns/tracking` and `/verification/product-aliases` were unreachable (each registered after a `/{int-param}` sibling) and are now declared before it. Full script suite re-run green against hermetic data. → ✅ | ✅ |
| **5** | **Align to the final division-company business model.** Single-company, division-scoped masters & operations per the FINAL architecture — without rebuilding the working AI/OCR/UPI automation. **Backend (`saas/`):** brand/product/chemist masters are strictly division-scoped (no NULL/global fallback — `list_brands`, `_assert_brand_in_div`, `_assert_product_in_div`, `list_products` filter `(p.division_id OR b.division_id)`, `_product_brand_id`, `_assert_product_unique` via `IS NOT DISTINCT FROM`, brand uniqueness + chemist duplicate rules drop `OR division_id IS NULL`). `product.*` permission module added to the catalog (`saas/tenant_schema.py` PERMISSION_CATALOG) and end-user roles now also view brands. Product create/update/delete/bulk/template all gate on **`product.manage`** (never `campaign.manage`); **brand is REQUIRED on product create (400)** and cannot be cleared on update; delete-protection returns **409 "Product has historical/campaign usage and cannot be deleted. Deactivate it instead."** (`campaign_products` + `pob_activities` reference checks) and brands are protected while they have products. Verification agents are read-only reviewers: `/{vid}/correct` + `/run-pipeline` now require **`verification.manage`**, `pob re-extract` returns **403** for `verifier`/`verification_agent` roles before any other path — agents keep approve/reject/duplicate/re-open and view their pipeline/history. Gratifications expose the chemist's UPI (`ch.id chemist_id, ch.upi_id chemist_upi_id` in `_grat_query`), `_mask_payment_fields` masks `chemist_upi_id` too, and cashback approval falls back to the chemist's **confirmed** UPI when no address is supplied. UPI save now persists `qr_type` (from `parse_upi_payload`) + `merchant_name`, records scan provenance (`upi_scan_source`, `upi_payee_name`, `upi_confirmed_by/at`), and `parse_upi_payload` now derives `qr_type` from the netloc of `upi://pay?...`. Idempotent replacement migration block at the end of TENANT_PATCHES adds `product.manage` + role grants, the new gratification types, the chemists UPI-confirmation columns, plus the **FINAL** chemist classification seeds (7 attachment types `individual…others`, 5 potential categories `a_plus…new`) and gratification-type seeds (`e_voucher`, `reward_points`) — `ON CONFLICT DO NOTHING` so existing tenants converge without destroying legacy codes. **Frontend (`web/`):** new `html5-qrcode` dependency + `src/QrScanner.jsx` camera scanner; `Chemists.jsx` `UpiScanForm` gains "📷 Scan UPI QR with camera" (paste fallback kept, existing-UPI replace label); `Gratification.jsx` cashback/UPI detail gains a Chemist-UPI panel (masked existing UPI + Use existing / Scan new with explicit replace confirmation); `VerificationAgent.jsx` removes the correction form, re-extract and run-pipeline controls (history stays read-only); stale "super-admin-managed/read-only masters" copy removed from `App.jsx` redirect + `Masters.jsx` brands are now `brand.manage`-gated CRUD. **Cross-check follow-ups (gap audit):** E-Voucher + Reward Points are now first-class lifecycles — voucher `generate→send→redeem` accepts `e_voucher` and approve/pay accept `reward_points` (UPI only required for cashback/upi, not points); the campaign wizard scheme dropdown reads configurable types from `/api/v1/gratification/types` (static fallback preserved), so E-Voucher/Reward Points schemes are selectable; `Products.jsx` Brand field is required (was "Optional" text while the backend 400'd) with a client-side guard; the generic Masters CRUD page now enforces `required` fields client-side. **Tests:** `test_product_crud.py` updated to FINAL rules (brand-required 400, brand preserved on update, delete-409 + deactivate, bulk brand-required) and new hermetic `scripts/test_business_alignment.py` (52 checks: division isolation A↔B, brand/product gating, MR 403s, verifier 403s + approve retained, UPI decode/save/mask + cashback fallback, FINAL seeds, e-voucher + reward-points lifecycles) — both green on scratch tenant/platform, DBs dropped on exit. `test_campaign_multibrand` uses superadmin console routes and `smoke_test` seeds campaigns directly, so no other hermetic gate was affected. | ✅ |

## Acceptance criteria per batch

- Every batch keeps **both** current apps running (legacy keeps serving until Batch 4).
- No new imports across the `saas↔app` boundary (grep gate in Batch 1+).
- Schema changes go through TENANT_DDL/TENANT_PATCHES with a `SCHEMA_VERSION` bump (the pob_required incident must not repeat).
- Each migrated feature passes the existing E2E/verify scripts or a ported equivalent.

## Batch 0 audit (context)

Before: `saas→app` imports at `platform_db.py`, `provision.py`, `routers/auth.py`,
`routers/recovery.py`, `routers/company.py`, `routers/superadmin.py`,
`scheduler.py`; plus `app→saas` imports in the two legacy routers
(`saas.upload_validation`). The hashing dependency is behavioral, not
incidental: pre-SaaS user rows still hold unsalted SHA-256 hashes, so the
merged `verify_pw` keeps the dual-format check.