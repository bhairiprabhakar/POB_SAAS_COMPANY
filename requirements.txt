fastapi>=0.115,<0.116        # pinned: 0.13x/0.14x introduced a lazy include_router() path
                              # (fastapi.routing._IncludedRouter) that silently drops routes
                              # registered on a router after it was first wrapped -- confirmed via
                              # this app's own routers (e.g. superadmin.router, mfa.router) landing
                              # 404/405 in production while working fine under TestClient. Do not
                              # loosen this cap without re-verifying every router's route count.
uvicorn[standard]>=0.30
starlette>=0.46,<0.47         # matches the fastapi 0.115.x line's tested starlette version
python-multipart>=0.0.9
psycopg2-binary>=2.9
werkzeug>=3.0               # password hashing (generate_password_hash/check_password_hash)
openpyxl>=3.1
google-genai>=0.3.0         # Gemini extraction (model chosen per-document, see saas/ai/gemini_extraction.py)
pypdf>=5.0                   # PDF page counting, text-layer detection, chunk splitting for large documents
pdfminer.six>=20231228       # cross-validation for text-layer detection (see saas/ai/gemini_extraction.py)
PyMuPDF>=1.24                # cross-validation for text-layer detection (imported as `fitz`)
Pillow>=10.0                 # EXIF-orientation auto-correction for uploaded photos
requests>=2.31                # remote document import (S3/EC2/Drive/web links)
python-dotenv>=1.0

# ── SaaS platform (saas/) ──────────────────────────────────────────────────
PyJWT>=2.9                   # JWT access/refresh tokens for the SaaS control plane + tenants
httpx>=0.27                   # TestClient / live API smoke tests
